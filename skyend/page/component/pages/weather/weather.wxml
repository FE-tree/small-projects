<import src="../../../common/head.wxml" />
<import src="../../../common/foot.wxml" />
<view class="container">
    <template is="head" data="{{title: 'weacher'}}" />

    <view class="weather">
        <!-- <view>{{weatherData}}</view> -->
        <text>{{weatherData}}</text>
    </view>
	<view class="btn-group">
	    <button class="mini-btn" type="primary" size="mini" bindtap="callPhone">拨打电话</button>
	    <button class="mini-btn" type="primary" size="mini" bindtap="scanCode">扫二维码</button>
	</view>
    <template is="foot" />
</view>